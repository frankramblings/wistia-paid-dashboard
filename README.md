# Wistia Paid Dashboard

Performance dashboard for Wistia content — tracks YouTube Ads (A La Carte), YouTube Organic, TikTok, Instagram, and LinkedIn.

## Setup

Copy `.env.local.example` to `.env.local` and fill in credentials.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
